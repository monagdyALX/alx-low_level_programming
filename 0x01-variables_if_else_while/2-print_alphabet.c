#include <stdio.h>

/**
 * * Prorgram to print lowercase alphabet
 * * Return char alphabet
 * */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

