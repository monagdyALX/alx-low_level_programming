#include <stdio.h>

/**
 * * Prorgram to print lowercase alphabet
 * * Return char alphabet
 * */

int main(void)
{
	int alph=97; 
	for (alph; alph<123; alph++)
		putchar(alph);
	printf("\n");

	return (0);
}

